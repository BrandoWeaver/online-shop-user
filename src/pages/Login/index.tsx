import React from 'react';

import { Box } from '@mui/material';

import Login from './Login';

function index() {
  return (
    <Box sx={{ bgcolor: 'background.defalut' }}>
      <Login />
    </Box>
  );
}

export default index;
