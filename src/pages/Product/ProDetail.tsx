import { Box, Typography } from "@mui/material";

function ProDetail() {
  return (
    <Box>
      <Typography>Product Detail</Typography>
      {/* <SwipeableTextMobileStepper
        imageLength={allProduct?.images.length || 0}
        allImage={allProduct?.images}
      /> */}
    </Box>
  );
}

export default ProDetail;
